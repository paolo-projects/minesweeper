import MinesweeperBoard, {
    DisplayedCell,
    TestResult,
} from "./minesweeper/board";
import Matrix, { MatrixCell } from "./utils/matrix";

export { MinesweeperBoard, DisplayedCell, TestResult, Matrix, MatrixCell };
